name: offline_sync_data
description: Offline-first synchronization with a persistent SQLite queue for Flutter apps.
version: 0.1.0
topics:
  - offline-first
  - synchronization
  - sqflite
  - connectivity

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  internet_connection_checker_plus: ^3.0.0
  path: ^1.9.1
  sqflite: ^2.4.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.3.4+4

flutter:
